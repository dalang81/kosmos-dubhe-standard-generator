/* eslint-disable  */
import {RegistStandardEnum} from 'kosmos-dubhe-standard-utils';

{{#enums}}
class {{UpperCamelCategory}} {
    static CATEGORY = '{{Upper_Snake_Category}}';
    static CATEGORY_NAME = '{{categoryName}}';

    constructor(itemLabel, itemName, itemKey, itemValue, itemSeq, remark) {
        this.itemLabel = itemLabel;
        this.itemName = itemName;
        this.itemKey = itemKey;
        this.itemValue = itemValue;
        this.itemSeq = itemSeq;
        this.remark = remark;
    }

    getItemLabel() {
        return this.itemLabel;
    }

    getItemName() {
        return this.itemName;
    }

    getItemValue() {
        return this.itemValue;
    }

    getItemKey() {
        return this.itemKey;
    }

    getItemSeq() {
        return this.itemSeq;
    }

    getRemrk() {
        return this.remark;
    }

    getCategory() {
        return {{UpperCamelCategory}}.CATEGORY;
    }

    getCategoryName() {
        return {{UpperCamelCategory}}.CATEGORY_NAME;
    }
}
{{#items}}
{{UpperCamelCategory}}.{{itemLabel}} = new {{UpperCamelCategory}}('{{itemLabel}}', '{{itemName}}', '{{itemKey}}', '{{itemValue}}', {{itemSeq}}, '{{remark}}'); {{#remark}}//{{/remark}} {{remark}}
{{/items}}

/************************** {{UpperCamelCategory}} define end ***************************/

{{/enums}}

const {{clazz}} = {};

{{#enums}}
{{UpperCamelCategory}}.$ALL = [

{{#items}}
{{UpperCamelCategory}}.{{itemLabel}},
{{/items}}
];

Object.freeze({{UpperCamelCategory}});
export { {{UpperCamelCategory}} };

/************************** {{UpperCamelCategory}} export end ***************************/

{{/enums}}

{{#enums}}
{{clazz}}['{{UpperCamelCategory}}'] = {{UpperCamelCategory}}.$ALL;
{{clazz}}['{{Upper_Snake_Category}}'] = {{UpperCamelCategory}}.$ALL;
{{/enums}}

export default {{clazz}};
function FindEnum(type) {
    return type.$ALL || {{clazz}}[type];
}

function getName() {
    return "{{clazz}}";
}
{{clazz}}.FindEnum = FindEnum;
{{clazz}}.getName = getName;
RegistStandardEnum({{clazz}});

/* eslint-enable  */
